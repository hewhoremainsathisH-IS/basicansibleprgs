---
ansible-navigator:
  ansible:
    config: ansible.cfg 
  execution-environment:
    container-engine: podman
    enabled: false
    pull:
      policy: missing
logging:
  mode: stdout
  playbook-artifact:
    enable: false

